package edu.uchicago.cs.db.subattr.cli.components;

import org.junit.Test;

import java.io.File;
import java.net.URI;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

public class CatamaranExtractorTest {

    @Test
    public void extract() throws Exception {
//        String catamaranExe = new File(Thread.currentThread().getContextClassLoader()
//                .getResource("catamaran/catamaran").toURI()).getAbsolutePath();
//        CatamaranExtractor extractor = new CatamaranExtractor(catamaranExe);
//        URI input = Thread.currentThread().getContextClassLoader().getResource("pattern/pattern_miner_sample").toURI();
//
//        extractor.setFileProcessor(files -> {
//            assertTrue(files.length == 1);
//            assertEquals("pattern_miner_sample.catamaran_1.tsv", files[0].getName());
//        });
//
//        extractor.extract(input);
    }
}